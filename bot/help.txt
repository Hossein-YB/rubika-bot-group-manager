`!راهنما`
برای دریافت لیست دستورات ربات از این دستور استفاده کنید.

`!به روزرسانی مدیران`
برای اپدیت کردن لیست مدیران یک گروه

`!قفل `
بعد کلمه قفل نام چیزی که میخواهید قفل شود را ارسال کنید برای نمونه
!قفل لینک

`!لیست قفل ها`
برای دریافت لیست قفل ها از این دستور استفاده کنید.

`!بازکردن `
بعد کلمه بازکردن نام چیزی که میخواهید قفل ان باز شود را ارسال کنید برای نمونه
!بازکردن گیف

`!بازکردن همه`
برای باز کردن تمام قفل ها از این دستور استفاده کنید.

`!وضعیت `
برای دریافت وضعیت قفل های گروه از این دستور استفاده کنید.

`!بن`
کسی که میخواهید بن بشه روی پیامش ریپلای کنید و کلمه !بن رو ارسال کنید
کاربر اخراج میشود

`!ارتقا مقام`
با این دستور یک کاربر را عضو ادین های ربات کنید. فقط مدیر اصلی گروه

`!تنزل مقام`
برای حذف مدیر از لیست مدیران ربات فقط مدیر اصلی گروه